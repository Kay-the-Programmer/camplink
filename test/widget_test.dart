// Placeholder. Add widget/integration tests here.
// CampLinkApp depends on Firebase initialization, so it cannot run in unit
// tests without a Firebase test harness — target individual widgets/providers
// in isolation instead.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
